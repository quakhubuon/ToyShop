@model IEnumerable<ToyShop.HangSanXuat>
<dt>Hãng sản xuất</dt>
@foreach (var item in Model)
{
    <dd>
        <a href="@Url.Action("HangSanXuat", "Home", new {id=item.MaHangSanXuat})">@item.TenHangSanXuat</a>
    </dd>
}
